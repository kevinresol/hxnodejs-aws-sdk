package aws_sdk.mturk;

typedef CreateHITResponse = {
	/**
		Contains the newly created HIT data. For a description of the HIT data structure as it appears in responses, see the HIT Data Structure documentation.
	**/
	@:optional
	var HIT : HIT;
};
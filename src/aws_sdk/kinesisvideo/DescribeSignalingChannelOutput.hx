package aws_sdk.kinesisvideo;

typedef DescribeSignalingChannelOutput = {
	/**
		A structure that encapsulates the specified signaling channel's metadata and properties.
	**/
	@:optional
	var ChannelInfo : ChannelInfo;
};
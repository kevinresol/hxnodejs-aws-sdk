package aws_sdk.devicefarm;

typedef CreateNetworkProfileResult = {
	/**
		The network profile that is returned by the create network profile request.
	**/
	@:optional
	var networkProfile : NetworkProfile;
};
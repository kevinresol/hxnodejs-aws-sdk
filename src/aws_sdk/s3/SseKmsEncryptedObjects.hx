package aws_sdk.s3;

typedef SseKmsEncryptedObjects = {
	/**
		Specifies whether Amazon S3 replicates objects created with server-side encryption using a customer master key (CMK) stored in AWS Key Management Service.
	**/
	var Status : String;
};
package aws_sdk.sagemaker;

typedef ProcessingResources = {
	/**
		The configuration for the resources in a cluster used to run the processing job.
	**/
	var ClusterConfig : ProcessingClusterConfig;
};
package aws_sdk.sesv2;

typedef SendingOptions = {
	/**
		If true, email sending is enabled for the configuration set. If false, email sending is disabled for the configuration set.
	**/
	@:optional
	var SendingEnabled : Bool;
};
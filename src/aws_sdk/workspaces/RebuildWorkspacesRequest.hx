package aws_sdk.workspaces;

typedef RebuildWorkspacesRequest = {
	/**
		The WorkSpace to rebuild. You can specify a single WorkSpace.
	**/
	var RebuildWorkspaceRequests : RebuildWorkspaceRequests;
};
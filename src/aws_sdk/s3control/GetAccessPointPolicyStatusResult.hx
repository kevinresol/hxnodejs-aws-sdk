package aws_sdk.s3control;

typedef GetAccessPointPolicyStatusResult = {
	/**
		Indicates the current policy status of the specified access point.
	**/
	@:optional
	var PolicyStatus : PolicyStatus;
};
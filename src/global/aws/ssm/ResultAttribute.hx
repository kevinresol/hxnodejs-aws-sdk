package global.aws.ssm;

typedef ResultAttribute = {
	/**
		Name of the inventory item type. Valid value: AWS:InstanceInformation. Default Value: AWS:InstanceInformation.
	**/
	var TypeName : String;
};
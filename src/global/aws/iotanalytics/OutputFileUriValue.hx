package global.aws.iotanalytics;

typedef OutputFileUriValue = {
	/**
		The URI of the location where data set contents are stored, usually the URI of a file in an S3 bucket.
	**/
	var fileName : String;
};
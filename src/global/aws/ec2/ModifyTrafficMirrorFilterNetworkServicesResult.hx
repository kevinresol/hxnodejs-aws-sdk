package global.aws.ec2;

typedef ModifyTrafficMirrorFilterNetworkServicesResult = {
	/**
		The Traffic Mirror filter that the network service is associated with.
	**/
	@:optional
	var TrafficMirrorFilter : TrafficMirrorFilter;
};
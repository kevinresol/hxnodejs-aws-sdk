package global.aws.ec2;

typedef ModifyVpcEndpointConnectionNotificationResult = {
	/**
		Returns true if the request succeeds; otherwise, it returns an error.
	**/
	@:optional
	var ReturnValue : Bool;
};
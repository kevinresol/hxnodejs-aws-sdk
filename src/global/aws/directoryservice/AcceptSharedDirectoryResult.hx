package global.aws.directoryservice;

typedef AcceptSharedDirectoryResult = {
	/**
		The shared directory in the directory consumer account.
	**/
	@:optional
	var SharedDirectory : SharedDirectory;
};
package global.aws.devicefarm;

typedef GetRemoteAccessSessionResult = {
	/**
		A container that lists detailed information about the remote access session.
	**/
	@:optional
	var remoteAccessSession : RemoteAccessSession;
};
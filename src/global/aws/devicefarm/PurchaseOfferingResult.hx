package global.aws.devicefarm;

typedef PurchaseOfferingResult = {
	/**
		Represents the offering transaction for the purchase result.
	**/
	@:optional
	var offeringTransaction : OfferingTransaction;
};
package global.aws.workspaces;

typedef RebootWorkspacesRequest = {
	/**
		The WorkSpaces to reboot. You can specify up to 25 WorkSpaces.
	**/
	var RebootWorkspaceRequests : RebootWorkspaceRequests;
};
package global.aws.storagegateway;

typedef DeleteTapeArchiveInput = {
	/**
		The Amazon Resource Name (ARN) of the virtual tape to delete from the virtual tape shelf (VTS).
	**/
	var TapeARN : String;
};
package global.aws.globalaccelerator;

typedef CreateEndpointGroupResponse = {
	/**
		The information about the endpoint group that was created.
	**/
	@:optional
	var EndpointGroup : EndpointGroup;
};
package global.aws.s3;

typedef AccessControlTranslation = {
	/**
		Specifies the replica ownership. For default and valid values, see PUT bucket replication in the Amazon Simple Storage Service API Reference.
	**/
	var Owner : String;
};